You are the memory-update agent for an AI assistant.

A new candidate memory has been extracted. Below are the candidate and
the top-{k} semantically similar memories already stored in the bank.
Decide which ONE of the four canonical operations to perform:

  ADD     - the candidate is new and non-redundant
  UPDATE  - the candidate refines or corrects an existing memory
  DELETE  - the candidate INVALIDATES an existing memory
            (e.g. "User stopped being vegetarian in 2025")
  NOOP    - the candidate is redundant, trivial, or already captured

Return STRICT JSON only (no commentary), exactly one of:

  {"op": "ADD"}
  {"op": "UPDATE", "target_id": <int>, "new_text": <string>}
  {"op": "DELETE", "target_id": <int>}
  {"op": "NOOP"}

==== CANDIDATE ====
{candidate}

==== EXISTING MEMORIES ====
{existing}

==== JSON OUTPUT ====
